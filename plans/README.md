# Implementation Plans

Generated by the improve skill on 2026-06-11 at commit `86edd88`. Execute in the order below unless dependencies say otherwise. Each executor must read the selected plan fully before starting, honor its STOP conditions, run every verification command, and update the status row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
| --- | --- | --- | --- | --- | --- |
| 001 | [Establish a green verification baseline](001-establish-verification-baseline.md) | P1 | M | — | DONE (`d1a9606` on `advisor/001-establish-verification-baseline`) |
| 002 | [Remove the hardcoded Stronghold vault password](002-remove-hardcoded-stronghold-password.md) | P1 | M | 001 | TODO |
| 003 | [Gate debug surfaces out of production](003-gate-production-debug-surfaces.md) | P1 | S | 001 | TODO |
| 004 | [Finish the transactions vertical slice](004-finish-transactions-vertical-slice.md) | P1 | L | 001 | TODO |
| 005 | [Preserve soft-delete state on updates](005-preserve-soft-delete-state-on-updates.md) | P1 | S | 001 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- `001` should land first because the repo's declared gates are currently red and later plans need a reliable `pnpm check` or equivalent baseline.
- `002` and `003` can run in either order after `001`; both touch Stronghold-adjacent surfaces, so coordinate if executed in parallel.
- `004` can run after `001`. It may create transaction tests that make `005` easier, but it is not strictly required for category-side soft-delete work.
- `005` can run after `001`. If `004` has not landed, the executor may need to create a minimal transaction repository test harness.

## Verification baseline from audit

- `pnpm tsc:check` failed at commit `86edd88` with TypeScript errors in `AppSidebar.tsx`, `DatePicker.tsx`, `navigation.tsx`, `ThemeToggle.tsx`, and `storage.ts`.
- `pnpm lint` failed at commit `86edd88` with 10 errors and 16 warnings.
- `cargo test --workspace` did not complete in the advisor sandbox because `libsqlite3-sys` hit a permission error during its build script. Re-run in a normal dev environment before treating Rust tests as red.

## Findings considered and rejected or deferred

- Tighten Tauri CSP, remove unused shell permission, and bound log retention: high-value security work, but deferred because the user selected only findings 1-5 from the first table and those map to the baseline, Stronghold password, debug surfaces, transactions slice, and soft-delete fix.
- Make imports report skipped rows and generate IDs for missing import IDs: real data-integrity issue, deferred for a later plan because it was finding 6 in the table.
- Define parent-category delete cascade/reparent/block semantics: real correctness issue, deferred because it needs a product decision on destructive behavior.
- Reduce redundant category parent queries: real performance issue, deferred until correctness and security plans land.
- Fix publish workflow package-manager drift: real DX issue, deferred because it was not selected. It can be a small future plan.
- Clean unused dependencies and Cargo lockfile drift: real maintenance issue, deferred until the green baseline exists.
- Repair stale docs and agent guidance: real DX issue, deferred unless the maintainer wants documentation cleanup next.
- Add SQLite database-at-rest encryption: important but larger than the selected Stronghold password fix and requires a threat-model/product decision.
